# nl2sh 新手指南

nl2sh 可以用日常语言查看 Android 设备信息，也可以在你确认后执行修改操作。模型负责理解问题和提出操作；命令仍由本机检查风险，再按需要征求你的同意。

## 第一步：选择使用方式

| 你的情况 | 推荐入口 | 安装说明 |
|---|---|---|
| 已在 Android 上使用 Termux | 在 Termux 安装并运行 `nl2sh` | [Termux 使用说明](Termux使用说明.md) |
| 用电脑连接 Android 手机 | 下载发布包，运行自动选设备的启动脚本 | [电脑连接使用说明](使用说明.md) |

电脑连接方式需要先开启 Android 的 USB 调试并完成设备授权。启动脚本会检查设备连接和处理器架构；如果提示 `unauthorized`，请解锁手机并接受调试授权。首次使用不需要安装 Rust 或 Android NDK。

## 第二步：连接模型服务

启动后，终端欢迎内容末尾会显示 Web 地址。在同一网络的浏览器中打开该地址，点击“快速开始”。引导首选 DeepSeek，模型预填 `deepseek-flash`；也可选择 OpenRouter、OpenAI、Moonshot/Kimi、SiliconFlow、Ollama 或其他自定义服务。OpenAI 使用官方地址；其他自定义服务可在下一步填写 Base URL。随后填写模型名称及所需的 API Key，点击“保存并检查连接”。如果使用本机 Ollama，需要先在运行 nl2sh 的设备上准备模型服务，并填写该服务实际地址与模型名称。

也可以只用终端：输入 `/config`，在“服务”和“模型与智能体”中设置。API Key 是模型服务商提供的访问凭据；nl2sh 不会替用户创建或购买密钥。连接检查读取模型列表，不会执行设备命令。模型列表检查失败时，按页面提示检查密钥、设备网络和代理设置。

Web 页面默认监听所有 IPv4 接口且无需登录。打开页面的人可以查看会话并操作配置或提交任务，请只在自己信任的网络中使用，注意不要把页面地址公开给陌生人。

## 第三步：完成第一个任务

在 Web 空白对话中点选“查看手机存储”，再点“发送”；也可在终端直接输入“查看这台设备还剩多少存储空间”。这类任务只查看信息。页面会显示模型思考、工具执行及完成状态，最后给出结论和依据。

想查某个文件时，可以输入 `@` 选择路径，例如“`@照片目录里有哪些文件`”。它只把路径交给 Agent；文件内容仍由受限工具读取。

## 遇到确认窗口时

先读风险说明、检查依据和待执行内容，再决定“批准这一次”或“拒绝”。修改设备内容需要确认；高风险操作还需要输入 `CONFIRM`。编辑待执行内容后，程序会重新检查风险。看不懂具体操作时，选择“拒绝”，并改为先问“请解释这一步会做什么”。

## 常见问题

| 页面提示 | 先检查什么 |
|---|---|
| 模型服务未接受密钥 | 服务商是否选对、API Key 是否完整、是否已经失效 |
| 请求受限或额度不足 | 稍后重试，或查看服务商的额度与限额 |
| 连接超时 | Android 设备的网络、模型服务地址和代理设置 |
| 找不到设备命令或文件 | 文件路径、名称，以及设备是否提供所需命令 |

详细的安装排查分别见[电脑连接使用说明](使用说明.md)和[Termux 使用说明](Termux使用说明.md)。
